# WeatherGPT Backend — Python deps per plan.md Phase 0.2 + 1.2 + 3.1
# Agora Agents SDK (typed builders over REST) — prefers SDK per AGENTS.md #6
agora-agents>=2.2.0
# FastAPI + server
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9
httpx>=0.27.0
# Token generation (RTC + RTM)
agora-token-builder>=1.0.0
# IMD client caching (TTL 300s per AGENTS.md #6)
cachetools>=5.4.0
# Location resolver fuzzy matching
rapidfuzz>=3.9.0
# MCP server for IMD tools (FastMCP)
fastmcp>=2.0.0
mcp>=1.0.0
# Utils
python-dotenv>=1.0.0
tenacity>=8.5.0
# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx
respx>=0.21.0
